# Copia este archivo como ".env" y rellena tus propios valores.
# El archivo ".env" real NUNCA se sube a GitHub (está en .gitignore).
#
#   cp .env.example .env
#
# Aquí solo van claves y tokens (secretos que dependen de cada persona/entorno).

OPENAI_API_KEY=
HUGGINGFACE_TOKEN=
COHERE_API_KEY=
WANDB_API_KEY=
